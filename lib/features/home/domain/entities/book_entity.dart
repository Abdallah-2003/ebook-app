class BookEntity {
  final String? image, title, author;
  final num price, rating;

  const BookEntity({
    required this.image,
    required this.title,
    required this.author,
    required this.price,
    required this.rating, 
});
}
